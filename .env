# API Configuration
# VITE_API_URL=https://qperform-api.azurewebsites.net
VITE_API_URL=http://localhost:3001

# Microsoft Dataverse Web API Configuration
VITE_DATAVERSE_URL=https://orgdf3b8790.api.crm.dynamics.com/api/data/v9.2

# Authentication Mode
# Options:
#   'mock' - Use mock authentication for local development
#   'platform' - Use platform's built-in authentication (qsoftware.cloud)
#   'azure-ad' - Use custom Azure AD app registration
VITE_AUTH_MODE=mock

# Platform Authentication (for qsoftware.cloud deployment)
# When deployed to qsoftware.cloud, the platform provides user context
# No additional Azure AD registration needed

# Development Mode: Set to 'true' to skip authentication
VITE_USE_MOCK_AUTH=true
